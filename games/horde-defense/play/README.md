Godot web export output.

Copied from `godot-horde-demo` with `tools/copy-web-to-site.ps1`.
`index.wasm.gz` is served as `/index.wasm` by the site worker so the file stays under Cloudflare's 25 MB limit.
